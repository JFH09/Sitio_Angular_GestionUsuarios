import { ListaEstudiantesDirective } from './lista-estudiantes.directive';

describe('ListaEstudiantesDirective', () => {
  it('should create an instance', () => {
    const directive = new ListaEstudiantesDirective();
    expect(directive).toBeTruthy();
  });
});
